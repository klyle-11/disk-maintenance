# Disk Intelligence Backend Dependencies
# Install with: pip install -r requirements.txt

fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
sqlalchemy>=2.0.0

# Local workspace dependency — install as editable:
#   pip install -e ../../../du-hast-much  (or wherever du-hast-much lives)
du-hast-much>=0.1.0
